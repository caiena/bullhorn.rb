#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

# executing rake
docker-compose run --rm bullhorn bundle exec rake "$@"#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Do any other automated setup that you need to do here
